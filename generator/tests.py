#from django.test import TestCase
#from .models import CodeGeneration  # Import your model if needed
#from .views import generate_code  # Import the function to test
#class CodeGenerationTests(TestCase):
#    def test_generate_code(self):
#        # Test case for generate_code function
#        NL = "Click on Search Button"
#        generated_code = generate_code(NL)
#       self.assertIsInstance(generated_code, list)
#        self.assertTrue(len(generated_code) > 0)
#        self.assertTrue(all(isinstance(code, str) for code in generated_code))

# Create your tests here.
